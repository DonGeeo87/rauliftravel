FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY main_mvp.py .

EXPOSE 3103

CMD ["uvicorn", "main_mvp:app", "--host", "0.0.0.0", "--port", "3103"]
